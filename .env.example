# The canonical, publicly-reachable origin for this deployment (no trailing
# slash). Used for canonical links, Open Graph/Twitter absolute URLs,
# sitemap.xml, and robots.txt. Falls back to config/site.ts's siteConfig.url
# if unset — see src/lib/seo/site-url.ts.
NEXT_PUBLIC_SITE_URL=https://example.com
